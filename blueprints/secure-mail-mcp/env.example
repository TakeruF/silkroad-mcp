# Placeholders only. Provider and deployment configuration belongs in the implementation repository.
MAIL_IMAP_HOST=imap.example.com
MAIL_IMAP_PORT=993
MAIL_SMTP_HOST=smtp.example.com
MAIL_SMTP_PORT=465
MAIL_ACCOUNT=you@example.com
MAIL_AUTH_SECRET=replace-with-provider-issued-secret
MCP_TRANSPORT=stdio
# For a remote deployment, configure a stable public URL and OAuth issuer in deployment secrets.
